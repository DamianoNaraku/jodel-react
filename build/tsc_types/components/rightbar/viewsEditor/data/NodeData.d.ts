/// <reference types="react" />
import { LViewElement } from '../../../../joiner';
interface Props {
    view: LViewElement;
    readonly: boolean;
}
declare function NodeData(props: Props): JSX.Element;
export default NodeData;
