import type { LProject } from '../../joiner';
export declare class Save {
    private static url;
    static project(p: LProject): Promise<void>;
    private static data;
    private static views;
    private static nodes;
}
