import { LClass, LModel, LPackage, LProject } from '../../../joiner';
export declare class StateMachine_M2 {
    static load(project: LProject): [LModel, LPackage, LClass, LClass, LClass, LClass, LClass];
    private static create;
    private static createStateMachine;
    private static createState;
    private static createTransition;
    private static createAbstractEvent;
    private static createCommand;
    private static createEvent;
}
